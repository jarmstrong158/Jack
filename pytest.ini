[pytest]
testpaths = tests
# volt_sim lives at the repo root and is not installed as a package, so the
# root has to be on sys.path. `python -m pytest` happens to add the cwd and so
# masked this; bare `pytest` did not, and failed collection outright.
pythonpath = .
